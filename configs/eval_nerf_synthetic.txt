expname = eval_nerf_synthetic
rootdir = /root/...  # REPLACE THIS with YOUR PATH.
ckpt_path = /root/...  # REPLACE THIS with YOUR PATH.
distributed = False

## eval options
eval_dataset = nerf_synthetic
eval_scenes = lego

num_source_views = 3

## CFF settings (must match fine-tune config exactly)
use_cff = True
cff_resolutions = [32, 64]
cff_feat_dim = 8
cff_out_dim = 16

## Rendering settings
chunk_size = 4096
N_importance = 64
N_samples = 64
inv_uniform = False
white_bkgd = True